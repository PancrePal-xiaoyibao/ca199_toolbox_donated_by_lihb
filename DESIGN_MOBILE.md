# CA19-9 Toolbox (小图宝) 移动端产品设计全案

## 1. 品牌定义与视觉规范
- **品牌背书**：小胰宝社区 (PancrePal Community)
- **子品牌**：小图宝 (医疗数据本地化可视化工具)
- **视觉元素**：
  - **Logo**：顶部显著位置适配小胰宝官方 Logo。
  - **色彩**：采用温暖、专业的医疗辅助色调（建议：柔和蓝/白/灰）。
  - **致谢**：首页底部常驻文字“感谢志愿者公益开源 ❤️”。

## 2. 页面结构与交互流程

### 2.1 强制授权层 (Privacy Gate)
- **交互逻辑**：App 每次冷启动（或从后台切回）时，必须弹出全屏/半屏授权窗口。
- **内容要求**：
  - **风险提醒**：明确告知医疗数据的敏感性。
  - **合规承诺**：强调本工具不联网、不存储、不上传。
  - **数据原理**：解释数据仅存在于临时内存，退出即焚。
- **控制逻辑**：只有点击“同意授权并开始”后，主功能页面才会被加载。

### 2.2 数据加载首页 (Dashboard)
- **功能点**：
  - **核心区**：显示“加载数据包 (.pcp)”按钮。
  - **引导区**：显示如何从 PC 端导出数据并发送至微信的操作动图/文字。
  - **品牌区**：显示小胰宝社区公益声明。

### 2.3 可视化画布 (Visualization Canvas)
- **数据关联**：自动读取并对齐 `data.csv` (指标)、`medication.csv` (用药记录)、`remarksline_update.csv` (阶段备注)。
- **渲染规格**：
  - **主视图**：CA19-9 时间趋势折线图。
  - **事件层**：在折线上方/下方通过图标或垂直线标注用药变更点。
  - **详情层**：点击数据点，浮窗显示该日期的数值及对应的备注信息。

## 3. 数据安全与隐私保障 (核心技术底线)

### 3.1 物理级安全隔离
- **网络限制**：在 App 编译层彻底移除所有网络权限（禁止 `INTERNET` 权限）。
- **零云端**：不设注册登录，不设云端同步，数据仅在用户自有设备间通过微信流转。

### 3.2 内存管理 (阅后即焚)
- **即时清理**：当系统触发 `onPause` 或 `onDestroy` 事件时，程序必须立即释放包含 CSV 数据的所有对象引用，并强制执行垃圾回收。
- **后台不保留**：严禁将 CSV 内容写入本地持久化存储（如 SharedPreferences 或 Local Database）。

### 3.3 无敏运行日志 (Anonymous Logging)
- **日志规范**：仅允许记录程序运行状态（如：`File loaded`），严禁在日志中输出任何与医疗数值相关的具体内容（如：`Value: 37.5`）。

## 4. 数据接力规格
- **PC 导出端**：需提供一个一键打包脚本，将 3 个 CSV 校验并封装为 `.pcp` 格式。
- **中转通道**：微信文件传输助手（推荐，作为用户最信任的私密通道）。
- **手机接收端**：App 必须具备“分享接收”能力，直接从系统文件流中解析数据。

## 5. 公益与合规声明
- 本项目由小胰宝社区志愿者捐赠开发。
- 代码完全开源，遵循 MIT/Apache 协议，接受社区审计。
- 声明：本工具仅作可视化辅助参考，不作为临床医疗诊断依据。
